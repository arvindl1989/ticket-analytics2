[build]
builder = "NIXPACKS"

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[deploy]
startCommand = "cd backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/sla-rules"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
