[build]
builder = "NIXPACKS"

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[deploy]
startCommand = "sh -c 'uvicorn main:app --app-dir backend --host 0.0.0.0 --port $PORT'"
restartPolicyType = "ON_FAILURE"
